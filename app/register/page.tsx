import LoginPage from '../login/page';

export default function RegisterPage() {
    return <LoginPage />;
}
// For MVP, Register and Login are same (OTP flow upserts user). 
// Keeping separate route for marketing/SEO purposes.
